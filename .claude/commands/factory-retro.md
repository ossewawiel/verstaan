---
description: Read docs/factory/lessons.jsonl, propose promoting repeated failures into real rules, and propose pruning stale ones. Never applies a change without approval.
---

Run the `factory-retro` skill. Propose only. Wait for a yes per proposal. Apply each approved
change in its own `chore: retro` commit.
