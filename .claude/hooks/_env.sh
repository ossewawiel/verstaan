#!/usr/bin/env bash
# Sourced by the other hooks, never run. Wrapper only. The logic is tools/factory/hooks/_env.sh,
# a tracked script. This file is edited by hand, never by an agent.
# shellcheck source=../../tools/factory/hooks/_env.sh
. "$(dirname "$0")/../../tools/factory/hooks/_env.sh"
