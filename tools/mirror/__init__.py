"""Scaffolding for issue 01 (prove the gate ladder).

The real mirror (SPEC.md §3.1) arrives with milestone M1. This module exists only so the
tooling gates have one Python package and one pytest to exercise.
"""


def placeholder_answer() -> int:
    return 42
