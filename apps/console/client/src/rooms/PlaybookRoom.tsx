// SPDX-License-Identifier: MPL-2.0
import { DocPage } from './DocPage';

export function PlaybookRoom() {
  return (
    <DocPage
      path="docs/factory/playbook.md"
      band="Playbook · how the game is played"
      lede="The moving parts as a player meets them: the map, an encounter, the gates, the checkpoints, and how the factory levels up."
    />
  );
}
