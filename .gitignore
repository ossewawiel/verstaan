# Personal mail export accidentally saved here. Never commit.
UNL Project Archive Now Available*
*.eml

# Secrets and local settings
.env
.env.*
.claude/settings.local.json
secrets/

# Build output
build/
out/
cmake-build-*/
# Per-machine CMake presets (e.g. a Linux worktree's linux-gcc/linux-clang presets, "Linux gate
# presets" -- docs/factory/issues/100-the-console-acts.md). Was excluded only via the untracked,
# per-clone .git/info/exclude; on a fresh clone without that local exclude, copying this file in
# by hand left the tree "dirty" by git's own reckoning, which tools/factory/hooks/gate_full.sh
# refuses to stamp.
CMakeUserPresets.json
*.obj
*.o
*.pdb
*.ilk
*.exe
*.dll
*.so
*.dylib

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Mirror scratch (the curated mirror under data/archive/ IS committed)
data/archive/_raw/
data/archive/_tmp/

# Editors
.vscode/
.idea/
*.swp

# Playwright MCP scratch
.playwright-mcp/

# The file console is a rendering of the files and git, regenerated by hooks. Never commit it.
docs/factory/console/

# The console app (apps/console, ADR 0010): npm dependencies and build output, never committed.
apps/console/node_modules/
apps/console/dist/
apps/console/dist-server/
apps/console/test-results/
apps/console/playwright-report/
apps/console/blob-report/

# Git worktrees, one per branch (side quest 92)
.worktrees/
