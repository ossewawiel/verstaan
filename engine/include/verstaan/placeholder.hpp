#pragma once

// Scaffolding for issue 01 (prove the gate ladder). Not the public API in
// SPEC.md §3.4 — that arrives with the real engine. This header exists only so
// verstaan_core has one function to build and one fast test to exercise.

namespace verstaan {

int placeholder_answer();

} // namespace verstaan
