// Placeholder for the `tier` CTest label (docs/standards/testing.md). Real tier budget tests
// (RAM, dictionary size) land at M4 onward (SPEC.md §3.5, docs/standards/testing.md). This keeps
// `ctest -L tier` runnable in the meantime. Not in scope for issue 02: any translation behaviour.

int main() { return 0; }
