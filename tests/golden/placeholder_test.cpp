// Placeholder for the `golden` CTest label (docs/standards/testing.md). Real golden sentences
// arrive with the compiler and the first store (tools/compiler, later M0/M1 issues); this keeps
// `ctest -L golden` runnable in the meantime. Not in scope for issue 02: any translation
// behaviour.

int main() { return 0; }
