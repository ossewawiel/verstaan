// Placeholder for the `equivalence` CTest label (docs/standards/testing.md). Real equivalence
// tests (runtime vs generated tables, ADR 0007) arrive once both rule back ends exist. This keeps
// `ctest -L equivalence` runnable in the meantime. Not in scope for issue 02: any translation
// behaviour.

int main() { return 0; }
